class Solution {
    int missingNum(int arr[]) { 
        int sum=0;
        int sum2=0;
        for(int i=1;i<=arr.length+1;i++){
            sum+=i;
        }
        for(int i=0;i<arr.length;i++){
            sum2+=arr[i];
        }
        return sum -sum2;
        
  }
}
