class Solution {
    public boolean isPalindrome(String s) {
        s=s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
        String so="";
        for(int i=s.length()-1;i>=0;i--){
            so=so+s.charAt(i);
        }
         return s.equals(so);
        }
    }
